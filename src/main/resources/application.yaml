spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/db_autism?useSSL=false&serverTimezone=UTC
    username: root
    password: mysql

server:
  port: 8888

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.join.autism.entity

---
spring:
  profiles: prod
  datasource:
    url: jdbc:mysql://47.103.10.220/db_autism?useSSL=false&serverTimezone=UTC&charactorEncoding=utf-8
    username: root
    password: Join666
server:
  port: 6666
